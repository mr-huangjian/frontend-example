import { createApp, computed } from 'vue'
import App from './App.vue'
import './index.css'

const app = createApp(App)
app.mount('#app')

// console.dir(app.config.globalProperties)
// app.config.globalProperties).$http = axios
